{
    "title": "SELECT dept, avg WHERE dept < HIST",
    "query": {
        "WHERE": {
            "LT": {
                "courses_dept": "hist"
            }
        },
        "OPTIONS": {
            "COLUMNS": [
                "courses_dept",
                "courses_avg"
            ]
        }
    },
    "isQueryValid": false,

    "result":["Invalid query because dept cannot be LT a string"]
}

