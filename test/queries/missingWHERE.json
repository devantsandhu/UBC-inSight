{
    "title": "SELECT dept, avg WHERE missing",
    "query": {

        "OPTIONS": {
            "COLUMNS": [
                "courses_dept",
                "courses_avg"
            ]
        }
    },
    "isQueryValid": false,

    "result":["Invalid query because WHERE missing"]
}

