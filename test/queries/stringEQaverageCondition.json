{
    "title": "SELECT dept, avg WHERE avg = poop",
    "query": {
        "WHERE": {
            "GT": {
                "courses_avg": "poop"
            }
        },
        "OPTIONS": {
            "COLUMNS": [
                "courses_dept",
                "courses_avg"
            ]
        }
    },
    "isQueryValid": false,

    "result":["Invalid query because condition input is a string"]
}

